{
  "name": "breakthebeat",
  "version": "1.0.0",
  "description": "Breakthebeat Entertainment - Professional Dance & Talent Agency",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"WEB,API\" -c \"cyan,magenta\" \"npm run dev -w web\" \"npm run start:dev -w api\"",
    "build": "npm run build -w web && npm run build -w api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Breakthebeat",
  "license": "ISC",
  "devDependencies": {
    "@types/nodemailer": "^7.0.11",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemailer": "^8.0.1"
  }
}
